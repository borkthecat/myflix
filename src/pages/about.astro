<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>About Me - MyFlix</title>
    <link rel="stylesheet" href="/styles/about.css" />
  </head>

  <body>
    <!-- Navbar -->
    <header class="navbar">
      <div class="navbar-left">
        <img src="/images/logo.png" alt="MyFlix Logo" class="logo" />
        <nav class="nav-links">
          <a href="/home">Home</a>
          <a href="/about">About Me</a>
          <a href="/home#skills">Skills</a>
          <a href="/home#languages">Languages</a>
          <a href="/home#projects">Projects</a>
        </nav>
        <div class="browse-menu">
          <button class="browse-btn">Browse</button>
          <div class="browse-list">
            <a href="/home">Home</a>
            <a href="/about">About Me</a>
            <a href="/home#skills">Skills</a>
            <a href="/home#languages">Languages</a>
            <a href="/home#projects">Projects</a>
          </div>
        </div>
      </div>
      <div class="navbar-right">
        <div class="search-wrapper">
          <input
            type="text"
            class="search-input"
            placeholder="Search site..."
          />
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="icon search-toggle"
            viewBox="0 0 24 24"
            fill="currentColor"
          >
            <path
              fill-rule="evenodd"
              clip-rule="evenodd"
              d="M10 2a8 8 0 015.292 13.708l4 4a1 1 0 01-1.414 1.414l-4-4A8 8 0 1110 2zm-6 8a6 6 0 1110.416 4.243A6 6 0 014 10z"
            ></path>
          </svg>
        </div>
        <img src="/images/user1.jpg" alt="Profile" class="avatar" />
      </div>
    </header>

    <!-- Main Content -->
    <main class="main-content">
      <!-- Hero Section -->
      <section class="about-hero">
        <div class="hero-bubbles">
          <div class="bubble bubble-1"></div>
          <div class="bubble bubble-2"></div>
          <div class="bubble bubble-3"></div>
          <div class="bubble bubble-4"></div>
          <div class="bubble bubble-5"></div>
        </div>
        <div class="hero-wrapper">
          <div class="hero-content">
            <!-- Animated Media Player -->
            <div class="talking-player">
              <div class="media-player-container" id="media-player">
                <div class="player-screen">
                  <div class="screen-display">
                    <div class="waveform">
                      <div class="wave-bar" style="--delay: 0s; --height: 40%"></div>
                      <div class="wave-bar" style="--delay: 0.1s; --height: 70%"></div>
                      <div class="wave-bar" style="--delay: 0.2s; --height: 30%"></div>
                      <div class="wave-bar" style="--delay: 0.3s; --height: 90%"></div>
                      <div class="wave-bar" style="--delay: 0.4s; --height: 50%"></div>
                      <div class="wave-bar" style="--delay: 0.5s; --height: 80%"></div>
                      <div class="wave-bar" style="--delay: 0.6s; --height: 35%"></div>
                      <div class="wave-bar" style="--delay: 0.7s; --height: 65%"></div>
                    </div>
                    <div class="track-info">
                      <div class="track-title">Portfolio Mix</div>
                      <div class="track-artist">Arai Nanami</div>
                    </div>
                  </div>
                </div>
                <div class="player-controls">
                  <button class="control-btn prev-btn">
                    <svg viewBox="0 0 24 24" fill="currentColor">
                      <path d="M6 6h2v12H6zm3.5 6l8.5 6V6z"/>
                    </svg>
                  </button>
                  <button class="control-btn play-btn" id="play-btn">
                    <svg class="play-icon" viewBox="0 0 24 24" fill="currentColor">
                      <path d="M8 5v14l11-7z"/>
                    </svg>
                    <svg class="pause-icon" viewBox="0 0 24 24" fill="currentColor" style="display: none;">
                      <path d="M6 4h4v16H6zm8 0h4v16h-4z"/>
                    </svg>
                  </button>
                  <button class="control-btn next-btn">
                    <svg viewBox="0 0 24 24" fill="currentColor">
                      <path d="M6 18l8.5-6L6 6v12zM16 6v12h2V6h-2z"/>
                    </svg>
                  </button>
                </div>
                <div class="player-progress">
                  <div class="progress-bar">
                    <div class="progress-fill" id="progress-fill"></div>
                  </div>
                  <div class="time-display">
                    <span class="current-time">0:00</span>
                    <span class="total-time">3:45</span>
                  </div>
                </div>
              </div>
              <div class="speech-bubble-main" id="speech-bubble">
                <div class="speech-content-main">
                  <p class="speech-text-main" id="speech-text">Hello! Welcome to my portfolio...</p>
                  <div class="typing-indicator">
                    <span></span>
                    <span></span>
                    <span></span>
                  </div>
                </div>
                <div class="speech-tail-main"></div>
              </div>
            </div>
            
            <div class="hero-subtitle-wrapper">
              <p class="hero-subtitle">Computer Science & Design Student</p>
              <div class="hero-line"></div>
            </div>
            <p class="hero-description">
              Undergraduate at SUTD studying Computer Science and Design, with a
              curiosity for technology and how it shapes the world. I'm passionate about
              creating innovative solutions that bridge the gap between design and engineering.
            </p>
          </div>
        </div>
      </section>

      <!-- Parallax Image Gallery -->
      <section class="parallax-gallery">
        <div class="parallax-container">
          <div class="parallax-track" id="parallax-track">
            <div class="parallax-image" style="background-image: url('/images/user1.jpg');" data-text="Moments of focus and creativity">
              <div class="parallax-overlay">
                <p class="parallax-text">Moments of focus and creativity</p>
              </div>
            </div>
            <div class="parallax-image" style="background-image: url('/images/user2.jpg');" data-text="Exploring new perspectives">
              <div class="parallax-overlay">
                <p class="parallax-text">Exploring new perspectives</p>
              </div>
            </div>
            <div class="parallax-image" style="background-image: url('/images/user3.jpg');" data-text="Building something meaningful">
              <div class="parallax-overlay">
                <p class="parallax-text">Building something meaningful</p>
              </div>
            </div>
            <div class="parallax-image" style="background-image: url('/images/user4.jpg');" data-text="Learning and growing">
              <div class="parallax-overlay">
                <p class="parallax-text">Learning and growing</p>
              </div>
            </div>
            <div class="parallax-image" style="background-image: url('/images/jellyfishfield.gif');" data-text="Finding inspiration in motion">
              <div class="parallax-overlay">
                <p class="parallax-text">Finding inspiration in motion</p>
              </div>
            </div>
            <div class="parallax-image" style="background-image: url('/images/jellyfishfield1.gif');" data-text="Embracing the journey">
              <div class="parallax-overlay">
                <p class="parallax-text">Embracing the journey</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Story Section - Creative Layout -->
      <section class="story-section">
        <div class="story-container">
          <div class="story-item story-item-creative">
            <div class="story-visual">
              <div class="story-icon">🎓</div>
            </div>
            <div class="story-content">
              <span class="story-label">01</span>
              <h2>Education</h2>
              <p>
                Currently pursuing my undergraduate degree at SUTD, where I'm
                studying Computer Science and Design. This unique combination
                allows me to bridge the gap between technical excellence and
                creative problem-solving.
              </p>
            </div>
          </div>

          <div class="story-item story-item-creative story-item-reverse">
            <div class="story-visual">
              <div class="story-icon">🔥</div>
            </div>
            <div class="story-content">
              <span class="story-label">02</span>
              <h2>Passion</h2>
              <p>
                I'm deeply curious about how technology shapes our world and
                impacts human experiences. My passion lies in creating innovative
                solutions that are both technically sound and beautifully designed.
              </p>
            </div>
          </div>

          <div class="story-item story-item-creative">
            <div class="story-visual">
              <div class="story-icon">🚀</div>
            </div>
            <div class="story-content">
              <span class="story-label">03</span>
              <h2>Goals</h2>
              <p>
                My goal is to work on projects that make a real difference,
                whether through user experience design, software development, or
                exploring the intersection of AI and creative technologies.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Interests Section -->
      <section class="interests-section">
        <h2 class="section-title">Beyond the Screen</h2>
        <div class="interests-container">
          <div class="interest-item">
            <h3>Fitness & Wellness</h3>
            <p>
              The gym is my sanctuary. It's where I recharge, stay focused,
              and maintain balance. Physical fitness keeps my mind sharp and
              ready for the next challenge.
            </p>
          </div>

          <div class="interest-item">
            <h3>Music</h3>
            <p>
              Music is my constant companion. Whether I'm coding, designing, or
              just reflecting, the right soundtrack helps me find flow and
              inspiration.
            </p>
          </div>

          <div class="interest-item">
            <h3>Experimentation</h3>
            <p>
              I love learning by doing. Experimenting with new technologies,
              trying different approaches, and turning ideas into reality is
              what drives me forward.
            </p>
          </div>
        </div>
      </section>

      <!-- GitHub & Experience Combined Section -->
      <section class="github-experience-section">
        <div class="combined-container">
          <!-- Experience Section (Left) -->
          <div class="experience-side">
            <h2 class="side-title">Experience</h2>
            <div class="experience-timeline-compact">
              <div class="timeline-item-compact">
                <div class="timeline-dot-compact"></div>
                <div class="timeline-content-compact">
                  <h3>Freelance Web Developer</h3>
                  <p class="timeline-company-compact">Self-Employed</p>
                  <p class="timeline-date-compact">December 2025 - Current</p>
                  <p class="timeline-desc-compact">Developing and maintaining websites for clients</p>
                </div>
              </div>
              <div class="timeline-item-compact">
                <div class="timeline-dot-compact"></div>
                <div class="timeline-content-compact">
                  <h3>Data Annotator</h3>
                  <p class="timeline-company-compact">Klass Engineering & Solutions</p>
                  <p class="timeline-date-compact">March 2025 - August 2025</p>
                  <p class="timeline-desc-compact">Annotated and processed data for machine learning projects</p>
                </div>
              </div>
              <div class="timeline-item-compact">
                <div class="timeline-dot-compact"></div>
                <div class="timeline-content-compact">
                  <h3>Web Developer Intern</h3>
                  <p class="timeline-company-compact">iClick Media Pte Ltd</p>
                  <p class="timeline-date-compact">September 2023 - February 2024</p>
                  <p class="timeline-desc-compact">Developed web applications and maintained existing websites</p>
                </div>
              </div>
            </div>
          </div>

          <!-- GitHub Section (Right) -->
          <div class="github-side">
            <div class="github-header-compact">
              <h2 class="side-title">GitHub</h2>
              <div class="github-status">
                <span class="status-dot"></span>
                <span class="status-text">Active</span>
              </div>
            </div>
            
            <div class="github-activity-compact">
              <div class="activity-graph-compact" id="github-graph">
                <div class="graph-loading">Loading activity...</div>
              </div>
              <div class="activity-stats-compact" id="github-stats">
                <span class="contributions-count">Loading...</span>
                <span class="contributions-text">contributions this year</span>
              </div>
            </div>

            <div class="github-repos-compact">
              <div class="repos-header-compact">
                <h3>Open Source</h3>
                <p class="repos-subtitle-compact">Templates & repos</p>
                <a href="https://github.com/borkthecat" target="_blank" rel="noopener noreferrer" class="view-all-btn">
                  View all
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M7 17L17 7M7 7h10v10"/>
                  </svg>
                </a>
              </div>
              <div class="repos-grid-compact" id="github-repos">
                <div class="repo-loading">Loading repositories...</div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Horizontal Text Section with Hover Images -->
      <section class="horizontal-text-section">
        <div class="horizontal-container">
          <div class="horizontal-word" data-image="/images/user1.jpg">
            <span>Design</span>
            <div class="hover-image"></div>
          </div>
          <div class="horizontal-word" data-image="/images/user2.jpg">
            <span>Code</span>
            <div class="hover-image"></div>
          </div>
          <div class="horizontal-word" data-image="/images/user3.jpg">
            <span>Create</span>
            <div class="hover-image"></div>
          </div>
          <div class="horizontal-word" data-image="/images/user4.jpg">
            <span>Innovate</span>
            <div class="hover-image"></div>
          </div>
        </div>
      </section>

      <!-- Philosophy Section -->
      <section class="philosophy-section">
        <div class="philosophy-wrapper">
          <blockquote class="philosophy-quote">
            "I believe in the power of learning, experimenting, and finding ways
            to turn ideas into something real and impactful."
          </blockquote>
          <div class="philosophy-stats">
            <div class="stat-item">
              <div class="stat-number" data-target="100">0</div>
              <div class="stat-label">Projects</div>
            </div>
            <div class="stat-item">
              <div class="stat-number" data-target="50">0</div>
              <div class="stat-label">Technologies</div>
            </div>
            <div class="stat-item">
              <div class="stat-number">∞</div>
              <div class="stat-label">Curiosity</div>
            </div>
          </div>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <footer class="footer">
      <div class="footer-container">
        <div class="socials">
          <a href="mailto:contact@example.com">
            <img src="/icons/mail.svg" alt="Email" />
          </a>
          <a href="#">
            <img src="/icons/instagram.svg" alt="Instagram" />
          </a>
          <a href="#">
            <img src="/icons/contact.svg" alt="Contact" />
          </a>
        </div>
        <div class="links">
          <a href="/about">About</a>
          <a href="/home#projects">Projects</a>
          <a href="/home#skills">Skills</a>
          <a href="/home#languages">Languages</a>
          <a href="/home#experience">Experience</a>
          <a href="mailto:contact@example.com">Contact Me</a>
          <a href="#">Privacy</a>
          <a href="#">Terms</a>
        </div>
        <div class="copyright">
          © <span id="year"></span> Arai Nanami Rachel. All rights reserved.
        </div>
      </div>
    </footer>

    <script>
      document.getElementById("year").textContent = new Date().getFullYear();

      document.documentElement.style.scrollBehavior = 'smooth';

      window.addEventListener('scroll', () => {
        const navbar = document.querySelector(".navbar");
        if (window.scrollY > 50) {
          navbar.classList.add("scrolled");
        } else {
          navbar.classList.remove("scrolled");
        }
      });

      // Animate numbers
      const animateNumbers = () => {
        const stats = document.querySelectorAll('.stat-number[data-target]');
        stats.forEach(stat => {
          const target = parseInt(stat.getAttribute('data-target'));
          const duration = 2000;
          const increment = target / (duration / 16);
          let current = 0;
          
          const updateNumber = () => {
            current += increment;
            if (current < target) {
              stat.textContent = Math.floor(current);
              requestAnimationFrame(updateNumber);
            } else {
              stat.textContent = target;
            }
          };
          
          const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
              if (entry.isIntersecting) {
                updateNumber();
                observer.unobserve(stat);
              }
            });
          }, { threshold: 0.5 });
          
          observer.observe(stat);
        });
      };

      // Animate elements on scroll
      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-in');
          }
        });
      }, observerOptions);

      document.querySelectorAll('.story-item-creative, .interest-item, .timeline-item-compact').forEach(el => {
        observer.observe(el);
      });

      // Horizontal text hover images - follow cursor
      const horizontalWords = document.querySelectorAll('.horizontal-word');
      const cursorImage = document.createElement('div');
      cursorImage.className = 'cursor-hover-image';
      document.body.appendChild(cursorImage);
      
      let currentImageUrl = '';
      let isHovering = false;
      
      horizontalWords.forEach(word => {
        const imageUrl = word.getAttribute('data-image');
        
        word.addEventListener('mouseenter', () => {
          currentImageUrl = imageUrl;
          isHovering = true;
          cursorImage.style.backgroundImage = `url(${imageUrl})`;
          cursorImage.style.opacity = '1';
        });
        
        word.addEventListener('mouseleave', () => {
          isHovering = false;
          currentImageUrl = '';
          cursorImage.style.opacity = '0';
        });
        
        word.addEventListener('mousemove', (e) => {
          if (isHovering && currentImageUrl === imageUrl) {
            cursorImage.style.left = e.clientX + 20 + 'px';
            cursorImage.style.top = e.clientY + 20 + 'px';
          }
        });
      });

      // Talking Player Animation
      const speechText = document.getElementById('speech-text');
      const speechBubble = document.getElementById('speech-bubble');
      const typingIndicator = speechBubble?.querySelector('.typing-indicator');
      
      const messages = [
        "Hello! Welcome to my portfolio...",
        "I'm passionate about design and code.",
        "Let's build something amazing together!",
        "Scroll down to see my work...",
        "Thanks for visiting!"
      ];
      
      let currentMessageIndex = 0;
      let isTyping = false;
      
      function typeMessage(message, callback) {
        if (isTyping) return;
        isTyping = true;
        
        if (typingIndicator) {
          typingIndicator.classList.add('active');
        }
        
        let charIndex = 0;
        speechText.textContent = '';
        
        const typeInterval = setInterval(() => {
          if (charIndex < message.length) {
            speechText.textContent += message[charIndex];
            charIndex++;
          } else {
            clearInterval(typeInterval);
            if (typingIndicator) {
              typingIndicator.classList.remove('active');
            }
            isTyping = false;
            if (callback) callback();
          }
        }, 50);
      }
      
      function showNextMessage() {
        if (currentMessageIndex < messages.length) {
          typeMessage(messages[currentMessageIndex], () => {
            setTimeout(() => {
              currentMessageIndex = (currentMessageIndex + 1) % messages.length;
              showNextMessage();
            }, 3000);
          });
        }
      }
      
      // Start talking when page loads
      setTimeout(() => {
        showNextMessage();
      }, 1000);
      
      // Media Player Interactions
      const mediaPlayer = document.querySelector('.media-player-container');
      const playBtn = document.getElementById('play-btn');
      const progressFill = document.getElementById('progress-fill');
      let isPlaying = false;

      if (playBtn) {
        playBtn.addEventListener('click', (e) => {
          e.stopPropagation();
          isPlaying = !isPlaying;
          const playIcon = playBtn.querySelector('.play-icon');
          const pauseIcon = playBtn.querySelector('.pause-icon');
          
          if (isPlaying) {
            playIcon.style.display = 'none';
            pauseIcon.style.display = 'block';
            progressFill.style.animation = 'progressMove 225s linear';
            progressFill.style.width = '0%';
            setTimeout(() => {
              progressFill.style.width = '100%';
            }, 10);
          } else {
            playIcon.style.display = 'block';
            pauseIcon.style.display = 'none';
            progressFill.style.animation = 'none';
          }
        });
      }

      if (mediaPlayer) {
        mediaPlayer.addEventListener('click', (e) => {
          if (e.target.closest('.control-btn') || e.target.closest('.progress-bar')) {
            return;
          }
          if (!isTyping) {
            currentMessageIndex = (currentMessageIndex + 1) % messages.length;
            showNextMessage();
          }
        });
      }

      // Enhanced Parallax Gallery Effect - Horizontal Scrolling
      const parallaxTrack = document.getElementById('parallax-track');
      const parallaxImages = document.querySelectorAll('.parallax-image');
      
      // Duplicate images for seamless loop
      if (parallaxTrack && parallaxImages.length > 0) {
        parallaxImages.forEach(img => {
          const clone = img.cloneNode(true);
          parallaxTrack.appendChild(clone);
        });
      }
      
      function updateParallax() {
        if (parallaxTrack) {
          const gallerySection = document.querySelector('.parallax-gallery');
          if (gallerySection) {
            const rect = gallerySection.getBoundingClientRect();
            const windowHeight = window.innerHeight;
            const isVisible = rect.top < windowHeight && rect.bottom > -200;
            
            if (isVisible) {
              // Calculate scroll progress
              const scrollY = window.scrollY;
              const sectionTop = scrollY + rect.top - windowHeight;
              const sectionHeight = rect.height + windowHeight;
              const scrollProgress = Math.max(0, Math.min(1, (scrollY - sectionTop) / sectionHeight));
              
              // Continuous horizontal scrolling
              const imageWidth = 260 + 18; // width + gap
              const totalWidth = imageWidth * parallaxImages.length;
              const maxScroll = totalWidth;
              const currentScroll = scrollProgress * maxScroll;
              
              parallaxTrack.style.transform = `translateX(-${currentScroll}px)`;
              
              // Reset position for seamless loop
              if (currentScroll >= totalWidth) {
                parallaxTrack.style.transform = `translateX(0px)`;
              }
            }
          }
        }
      }
      
      // Smooth parallax on scroll
      let ticking = false;
      window.addEventListener('scroll', () => {
        if (!ticking) {
          requestAnimationFrame(() => {
            updateParallax();
            ticking = false;
          });
          ticking = true;
        }
      });
      
      updateParallax();

      animateNumbers();

      // GitHub Integration
      const GITHUB_USERNAME = 'borkthecat';
      
      async function fetchGitHubData() {
        try {
          // Fetch user repos
          const reposResponse = await fetch(`https://api.github.com/users/${GITHUB_USERNAME}/repos?sort=updated&per_page=3`);
          const repos = await reposResponse.json();
          
          // Fetch user data for contribution count
          const userResponse = await fetch(`https://api.github.com/users/${GITHUB_USERNAME}`);
          const userData = await userResponse.json();
          
          // Display repos
          const reposGrid = document.getElementById('github-repos');
          if (reposGrid && repos.length > 0) {
            reposGrid.innerHTML = repos.slice(0, 3).map(repo => {
              const language = repo.language || 'Other';
              const languageClass = language.toLowerCase();
              const isFeatured = repo.stargazers_count > 0 || repo.name.includes('template') || repo.name.includes('portfolio');
              
              return `
                <a href="${repo.html_url}" target="_blank" rel="noopener noreferrer" class="repo-card">
                  <div class="repo-card-header">
                    <svg class="repo-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path>
                    </svg>
                    <h4 class="repo-name">
                      ${repo.name}
                      ${isFeatured ? '<span class="repo-featured">Featured</span>' : ''}
                    </h4>
                  </div>
                  <p class="repo-description">${repo.description || 'No description available.'}</p>
                  <div class="repo-tags">
                    ${repo.topics && repo.topics.length > 0 ? repo.topics.slice(0, 3).map(topic => 
                      `<span class="repo-tag">${topic}</span>`
                    ).join('') : ''}
                    ${!repo.topics || repo.topics.length === 0 ? `<span class="repo-tag">${language}</span>` : ''}
                  </div>
                  <div class="repo-footer">
                    <div class="repo-language">
                      <span class="language-dot ${languageClass}"></span>
                      <span>${language}</span>
                    </div>
                  </div>
                </a>
              `;
            }).join('');
          }
          
          // Display contribution graph placeholder (GitHub API doesn't provide graph data directly)
          const graphContainer = document.getElementById('github-graph');
          if (graphContainer) {
            // Create a simple contribution graph visualization
            const weeks = 53;
            const daysPerWeek = 7;
            let graphHTML = '<div style="display: flex; gap: 3px; flex-wrap: wrap; justify-content: center;">';
            
            for (let i = 0; i < weeks * daysPerWeek; i++) {
              const intensity = Math.floor(Math.random() * 4); // 0-3 intensity levels
              const colors = ['#161b22', '#0e4429', '#006d32', '#26a641', '#39d353'];
              graphHTML += `<div style="width: 10px; height: 10px; background: ${colors[intensity]}; border-radius: 2px;"></div>`;
            }
            graphHTML += '</div>';
            graphContainer.innerHTML = graphHTML;
          }
          
          // Display stats
          const statsContainer = document.getElementById('github-stats');
          if (statsContainer) {
            // Estimate contributions (GitHub API doesn't provide exact count)
            const publicRepos = userData.public_repos || 0;
            const estimatedContributions = Math.floor(publicRepos * 12.5); // Rough estimate
            
            statsContainer.innerHTML = `
              <span class="contributions-count">${estimatedContributions.toLocaleString()}</span>
              <span class="contributions-text">contributions this year</span>
            `;
          }
        } catch (error) {
          console.error('Error fetching GitHub data:', error);
          const reposGrid = document.getElementById('github-repos');
          if (reposGrid) {
            reposGrid.innerHTML = '<div class="repo-loading">Unable to load repositories. <a href="https://github.com/' + GITHUB_USERNAME + '" target="_blank" style="color: #3b82f6;">Visit GitHub</a></div>';
          }
        }
      }
      
      // Load GitHub data when page loads
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', fetchGitHubData);
      } else {
        fetchGitHubData();
      }

      const searchWrapper = document.querySelector(".search-wrapper");
      const searchToggle = document.querySelector(".search-toggle");
      const searchInput = document.querySelector(".search-input");

      if (searchToggle) {
        searchToggle.addEventListener("click", () => {
          searchWrapper.classList.toggle("active");
          if (searchWrapper.classList.contains("active")) {
            searchInput.focus();
          }
        });
      }

      document.addEventListener("click", (e) => {
        if (searchWrapper && !searchWrapper.contains(e.target)) {
          searchWrapper.classList.remove("active");
        }
      });

      document.addEventListener("DOMContentLoaded", () => {
        const browseMenu = document.querySelector(".browse-menu");
        const browseBtn = document.querySelector(".browse-btn");

        if (browseBtn) {
          browseBtn.addEventListener("click", () => {
            browseMenu.classList.toggle("active");
          });

          document.addEventListener("click", (e) => {
            if (browseMenu && !browseMenu.contains(e.target)) {
              browseMenu.classList.remove("active");
            }
          });
        }
      });
    </script>
  </body>
</html>
